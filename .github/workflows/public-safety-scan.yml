name: public-safety-scan

on:
  push:
  pull_request:

jobs:
  public-safety-scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - run: python tools/public_safety_scan.py . --strict
